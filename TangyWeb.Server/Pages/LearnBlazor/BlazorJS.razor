@page "/blazorJS"
@inject IJSRuntime _jsRuntime

<h3>BlazorJS</h3>

<div class="row">
    <button class="btn btn-secondary" @onclick=@TestConfirmBox>Test Confirm Box</button>
</div>
<div class="row">
    <button class="btn btn-success" @onclick=@ToastrSuccess>Toastr Success</button>
    <button class="btn btn-danger" @onclick=@ToastrFailure>Toastr Failure</button>
</div>

<div class="row">
    @if (confirmResult)
    {
        <p>Confirmation has been made</p>
    }
    else
    {
        <p>Confirmation pending!</p>
    }
</div>

@code {
    string confirmMessage = "Are you sure?";
    bool confirmResult;

    async Task TestConfirmBox() => confirmResult = await _jsRuntime.InvokeAsync<bool>("confirm", confirmMessage);

    async Task ToastrSuccess() => await _jsRuntime.ToastrSuccess("Success!");

    async Task ToastrFailure() => await _jsRuntime.ToastrFailure("Failure!!!");

}
